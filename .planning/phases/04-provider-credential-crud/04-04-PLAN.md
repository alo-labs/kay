---
phase: 04-provider-credential-crud
plan: 4
type: execute
wave: 2
depends_on:
  - 1
  - 3
files_modified:
  - code-rs/tui/src/bottom_pane/provider_credentials_view.rs
  - code-rs/tui/tests/vt100_chatwidget_snapshot.rs
autonomous: true
requirements:
  - PROVIDER-01
user_setup: []
must_haves:
  truths:
    - The provider pane can delete a selected provider credential and preserve the rest.
    - OpenAI delete behavior only clears the OpenAI credential path and leaves the existing login behavior intact.
    - VT100 snapshots cover the ordered provider list, the add/update state, and the delete-confirmation state without leaking raw keys.
  artifacts:
    - path: code-rs/tui/src/bottom_pane/provider_credentials_view.rs
      provides: Provider delete state and action handling
      contains: remove_provider_api_key
    - path: code-rs/tui/tests/vt100_chatwidget_snapshot.rs
      provides: TUI regression snapshots for provider CRUD
      contains: /provider
  key_links:
    - from: delete confirmation in the provider pane
      to: `remove_provider_api_key(...)`
      via: destructive credential removal
      pattern: `remove_provider_api_key`
    - from: provider-pane render output
      to: ordered provider list and masked key states
      via: VT100 snapshots
      pattern: `/provider`
---

<objective>
Finish provider deletion and lock the provider-pane behavior with snapshots.

Purpose: close the last CRUD gap after the `/provider` shell and add/update path are in place, while proving the UI stays ordered and redacted.
Output: delete confirmation in the provider pane plus snapshot coverage for the final provider-management flow.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-credential-crud/04-CONTEXT.md
@.planning/phases/04-provider-credential-crud/04-DISCUSSION-LOG.md
@code-rs/tui/src/bottom_pane/provider_credentials_view.rs
@code-rs/tui/tests/vt100_chatwidget_snapshot.rs
@code-rs/core/src/auth.rs

<interfaces>
From `code-rs/tui/src/bottom_pane/provider_credentials_view.rs`:

```rust
pub(crate) struct ProviderCredentialsView;
impl<'a> BottomPaneView<'a> for ProviderCredentialsView { /* ... */ }
```

From `code-rs/core/src/auth.rs`:

```rust
pub fn remove_provider_api_key(code_home: &Path, provider_ref: &str) -> std::io::Result<bool>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider deletion to the provider pane</name>
  <files>code-rs/tui/src/bottom_pane/provider_credentials_view.rs</files>
  <read_first>code-rs/tui/src/bottom_pane/provider_credentials_view.rs, code-rs/core/src/auth.rs</read_first>
  <action>Add the delete-confirmation and delete-execution path per D-03 using the core `remove_provider_api_key(...)` helper from plan 1. Keep the delete copy and confirmation flow aligned with the provider pane only, and do not broaden the scope into `/login` account deletion or Phase 5 `/model` behavior. For OpenAI, the delete action should only clear the provider credential state; do not change the existing login behavior for OpenAI, MiniMax, or OpenCode Go per D-07.</action>
  <verify>cargo test -p code-tui --test vt100_chatwidget_snapshot --features test-helpers -- --nocapture</verify>
  <done>The provider pane can remove a selected provider credential and preserve the rest.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot the final provider-management UX</name>
  <files>code-rs/tui/tests/vt100_chatwidget_snapshot.rs</files>
  <read_first>code-rs/tui/tests/vt100_chatwidget_snapshot.rs, code-rs/tui/src/bottom_pane/provider_credentials_view.rs</read_first>
  <action>Add VT100 snapshots that prove the final `/provider` UX: the ordered provider list, the add/update state, the delete-confirmation state, and the redacted key display. Keep the snapshots focused on observable UI state only; do not assert on raw secrets or Phase 5 model filtering.</action>
  <verify>cargo test -p code-tui --test vt100_chatwidget_snapshot --features test-helpers -- --nocapture</verify>
  <done>Provider CRUD has visual regression coverage and no raw credentials appear in snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Delete confirmation is a destructive path and must be explicit in the UI.
- Snapshot fixtures must not capture raw API keys or other secrets.
- The provider pane must not mutate `/login` account history or `/model` selection state.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-04-04-01 | tampering | `code-rs/tui/src/bottom_pane/provider_credentials_view.rs` | mitigate | Require an explicit delete-confirmation state and call the core delete helper only for the selected provider entry. |
| T-04-04-02 | information_disclosure | `code-rs/tui/tests/vt100_chatwidget_snapshot.rs` | mitigate | Snapshot only masked provider status and ordered list state, never raw API key text. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p code-tui --test vt100_chatwidget_snapshot --features test-helpers -- --nocapture`
</verification>

<success_criteria>

- Delete confirmation removes only the selected provider credential
- Other provider credentials remain intact after deletion
- OpenAI delete does not disturb the existing login behavior
- VT100 snapshots cover the final ordered provider UX without secrets

</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-credential-crud/04-04-SUMMARY.md`
</output>
