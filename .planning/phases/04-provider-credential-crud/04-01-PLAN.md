---
phase: 04-provider-credential-crud
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - code-rs/core/src/auth.rs
autonomous: true
requirements:
  - PROVIDER-01
user_setup: []
must_haves:
  truths:
    - Users can create, list, update, and delete provider API keys through core auth helpers without editing config files.
    - Saving one supported provider key leaves the other supported provider keys intact.
    - OpenAI keeps using the existing OpenAI login path while provider credential CRUD expands around it.
  artifacts:
    - path: code-rs/core/src/auth.rs
      provides: Provider credential read/write/delete helpers and regression tests
      contains: remove_provider_api_key
  key_links:
    - from: save_provider_api_key
      to: auth.json.provider_credentials
      via: atomic auth.json rewrite
      pattern: provider_credentials
    - from: remove_provider_api_key
      to: auth.json.provider_credentials
      via: targeted provider entry deletion
      pattern: provider_credentials
    - from: save_provider_api_key
      to: login_with_api_key
      via: OpenAI special case
      pattern: provider_ref == "openai"
---

<threat_model>
## Trust Boundaries

- CLI arguments and shell history are untrusted paths for secret input.
- `auth.json` is the only on-disk credential store this phase should mutate for provider API keys.
- Logging and test output must never print raw provider secrets.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-04-01-01 | information_disclosure | `code-rs/core/src/auth.rs` | mitigate | Keep provider-key persistence and deletion inside the existing atomic auth.json writer, and never log raw key material in helpers or tests. |
| T-04-01-02 | tampering | `code-rs/core/src/auth.rs` | mitigate | Normalize provider ids before mutation and delete only the targeted provider entry so unrelated provider credentials remain intact. |
</threat_model>

<objective>
Implement the core provider-credential storage operations.

Purpose: give the TUI and CLI a single, reliable storage layer for provider keys before the UI and command wiring land.
Output: auth helpers for provider-key deletion plus regression tests that prove provider entries survive add/update/delete cycles.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-credential-crud/04-CONTEXT.md
@.planning/phases/04-provider-credential-crud/04-DISCUSSION-LOG.md
@code-rs/core/src/auth.rs

<interfaces>
From `code-rs/core/src/auth.rs`:

```rust
pub fn save_provider_api_key(code_home: &Path, provider_ref: &str, api_key: &str) -> std::io::Result<()>;
pub fn try_read_auth_json(auth_file: &Path) -> std::io::Result<AuthDotJson>;
pub fn write_auth_json(auth_file: &Path, auth_dot_json: &AuthDotJson) -> std::io::Result<()>;
impl AuthDotJson {
    pub fn provider_api_key(&self, provider_ref: &str) -> Option<&str>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider-key deletion to the auth layer</name>
  <files>code-rs/core/src/auth.rs</files>
  <read_first>code-rs/core/src/auth.rs</read_first>
  <action>Add a `remove_provider_api_key(...)` helper per D-03 so provider credential CRUD can delete only the selected entry from `auth.json.provider_credentials`. Keep `save_provider_api_key(...)` and `login_with_api_key(...)` unchanged for OpenAI and non-OpenAI provider writes per D-07, and do not touch `auth_accounts.json` in this phase. Normalize provider ids before mutation and keep all writes on the existing atomic auth-file path so unrelated credentials survive add/update/delete cycles.</action>
  <verify>cargo test -p code-core auth -- --nocapture</verify>
  <done>Core provider credential storage can save, update, and delete without dropping unrelated provider keys.</done>
</task>

<task type="auto">
  <name>Task 2: Lock provider-credential CRUD invariants with unit tests</name>
  <files>code-rs/core/src/auth.rs</files>
  <read_first>code-rs/core/src/auth.rs</read_first>
  <action>Add or extend unit tests in `auth.rs` to prove provider-key save/update/delete round trips, provider keys survive OpenAI writes, and deleting one provider leaves the other supported provider credentials intact. Use D-03 and D-07 in the assertions so the tests document the delete scope and the preserved OpenAI behavior explicitly.</action>
  <verify>cargo test -p code-core auth -- --nocapture</verify>
  <done>Auth tests cover the CRUD edge cases and the OpenAI preservation rule.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p code-core auth -- --nocapture`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced in `code-rs/core/src/auth.rs`
- Provider credential writes, updates, and deletes preserve unrelated stored keys
- Existing OpenAI login behavior remains intact while the CRUD surface is added

</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-credential-crud/04-01-SUMMARY.md`
</output>
