---
phase: 04-provider-credential-crud
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - code-rs/tui/src/slash_command.rs
  - code-rs/tui/src/app/events.rs
  - code-rs/tui/src/chatwidget.rs
  - code-rs/tui/src/bottom_pane/mod.rs
  - code-rs/tui/src/bottom_pane/provider_credentials_view.rs
  - docs/slash-commands.md
autonomous: true
requirements:
  - PROVIDER-01
  - PROVIDER-02
user_setup: []
must_haves:
  truths:
    - `/provider` appears in the slash-command list and opens a dedicated provider pane.
    - The provider pane shows OpenCode Go, MiniMax, and OpenAI in that exact order.
    - The new provider surface supports listing plus add/update flows without changing `/login` or `/model`.
  artifacts:
    - path: code-rs/tui/src/slash_command.rs
      provides: Slash-command enum and parser entry for `/provider`
      contains: Provider
    - path: code-rs/tui/src/app/events.rs
      provides: Slash-command dispatch into ChatWidget
      contains: SlashCommand::Provider
    - path: code-rs/tui/src/chatwidget.rs
      provides: Provider-pane entrypoint from the chat widget
      contains: show_provider_credentials_view
    - path: code-rs/tui/src/bottom_pane/provider_credentials_view.rs
      provides: Provider list and add/update UI shell
      contains: OpenCode Go
    - path: docs/slash-commands.md
      provides: User-facing slash-command docs
      contains: /provider
  key_links:
    - from: `SlashCommand::Provider`
      to: `handle_provider_command()`
      via: slash-command dispatch
      pattern: `SlashCommand::Provider`
    - from: `handle_provider_command()`
      to: `show_provider_credentials_view()`
      via: provider-pane entrypoint
      pattern: `provider`
    - from: provider-pane save actions
      to: `auth::save_provider_api_key(...)`
      via: existing auth helper reuse
      pattern: `save_provider_api_key`
---

<objective>
Add the `/provider` TUI entry and its ordered provider-management shell.

Purpose: give users a visible provider-management surface before delete/regression polish lands, while keeping `/login` and `/model` behavior unchanged.
Output: a new `/provider` command, a dedicated provider pane, explicit provider ordering, and slash-command docs that match the UI.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-credential-crud/04-CONTEXT.md
@.planning/phases/04-provider-credential-crud/04-DISCUSSION-LOG.md
@code-rs/tui/src/slash_command.rs
@code-rs/tui/src/app/events.rs
@code-rs/tui/src/chatwidget.rs
@code-rs/tui/src/bottom_pane/mod.rs
@code-rs/tui/src/bottom_pane/login_accounts_view.rs
@code-rs/tui/src/bottom_pane/form_text_field.rs
@code-rs/core/src/auth.rs
@docs/slash-commands.md

<interfaces>
From `code-rs/tui/src/slash_command.rs`:

```rust
pub enum SlashCommand { /* ... */, Login, Logout, /* ... */ }
pub fn process_slash_command_message(message: &str) -> ProcessedCommand;
```

From `code-rs/tui/src/chatwidget.rs`:

```rust
pub(crate) fn handle_login_command(&mut self);
pub(crate) fn show_login_accounts_view(&mut self);
pub(crate) fn show_login_add_account_view(&mut self);
```

From `code-rs/tui/src/bottom_pane/mod.rs`:

```rust
pub fn show_login_accounts(&mut self, view: LoginAccountsView);
pub fn show_login_add_account(&mut self, view: LoginAddAccountView);
```

From `code-rs/core/src/auth.rs`:

```rust
pub fn save_provider_api_key(code_home: &Path, provider_ref: &str, api_key: &str) -> std::io::Result<()>;
pub fn try_read_auth_json(auth_file: &Path) -> std::io::Result<AuthDotJson>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `/provider` in command discovery and docs</name>
  <files>code-rs/tui/src/slash_command.rs, code-rs/tui/src/app/events.rs, docs/slash-commands.md</files>
  <read_first>code-rs/tui/src/slash_command.rs, code-rs/tui/src/app/events.rs, docs/slash-commands.md</read_first>
  <action>Add `SlashCommand::Provider` per D-01 and wire its dispatch in `app/events.rs` so `/provider` opens the provider-management flow instead of falling through to `/login` or `/model`. Keep the command list order sensible in the popup, add a focused unit test in `slash_command.rs` for `/provider`, and update `docs/slash-commands.md` so the command list matches the UI. Do not add any `/model` filtering or provider-model compatibility work here; Phase 5 stays deferred per D-06.</action>
  <verify>cargo test -p code-tui slash_command -- --nocapture</verify>
  <done>`/provider` is a recognized slash command and the docs list it in the UI order.</done>
</task>

<task type="auto">
  <name>Task 2: Add the provider-pane shell and ordered add/update flow</name>
  <files>code-rs/tui/src/chatwidget.rs, code-rs/tui/src/bottom_pane/mod.rs, code-rs/tui/src/bottom_pane/provider_credentials_view.rs</files>
  <read_first>code-rs/tui/src/chatwidget.rs, code-rs/tui/src/bottom_pane/mod.rs, code-rs/tui/src/bottom_pane/login_accounts_view.rs, code-rs/tui/src/bottom_pane/form_text_field.rs, code-rs/core/src/auth.rs</read_first>
  <action>Create a dedicated provider pane per D-01 and D-02 that renders OpenCode Go, MiniMax, and OpenAI in that exact order. Reuse the existing auth read/write helpers so the shell can list current provider-key presence and support add/update actions without editing config files, but leave delete handling to the next task. Keep `/login` on its existing account-management path, keep provider/model plugins orthogonal per D-06, and use the existing provider metadata for names and env-key hints instead of duplicating copy.</action>
  <verify>cargo test -p code-tui slash_command -- --nocapture</verify>
  <done>The `/provider` pane opens, shows the ordered provider set, and can add/update keys without disturbing `/login`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Slash commands are untrusted user input until parsed into a known enum variant.
- Provider status text shown in the TUI must not reveal raw API keys.
- The provider pane must not leak into `/model` behavior or model-routing code.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-04-03-01 | tampering | `code-rs/tui/src/slash_command.rs`, `code-rs/tui/src/app/events.rs` | mitigate | Add `SlashCommand::Provider` explicitly and route it directly to the provider pane so it cannot accidentally inherit `/login` or `/model` behavior. |
| T-04-03-02 | information_disclosure | `code-rs/tui/src/bottom_pane/provider_credentials_view.rs` | mitigate | Display only masked key status or presence state in the pane and reuse the existing auth helpers for reads instead of printing stored secrets. |
| T-04-03-03 | tampering | `code-rs/tui/src/bottom_pane/provider_credentials_view.rs` | mitigate | Keep provider ordering data-driven and explicit as OpenCode Go, MiniMax, OpenAI so the pane does not depend on HashMap iteration order. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p code-tui slash_command -- --nocapture`
</verification>

<success_criteria>

- `/provider` is discoverable in the slash-command popup and docs
- The provider pane opens from `/provider`
- The provider list order is OpenCode Go, MiniMax, OpenAI
- Add/update actions work without touching `/login`
- Phase 5 `/model` filtering remains deferred and untouched

</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-credential-crud/04-03-SUMMARY.md`
</output>
