---
phase: 04-provider-credential-crud
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - code-rs/cli/src/main.rs
  - code-rs/cli/src/login.rs
  - code-rs/cli/tests/provider_api_key_entry.rs
  - docs/authentication.md
autonomous: true
requirements:
  - AUTH-01
user_setup: []
must_haves:
  truths:
    - `code login --api-key <KEY>` is accepted as a direct argument path for scripted onboarding.
    - `--with-api-key` still reads the API key from stdin so piped workflows keep working.
    - Existing provider dispatch still routes OpenAI through the OpenAI login path and non-OpenAI providers through provider-key storage.
  artifacts:
    - path: code-rs/cli/src/main.rs
      provides: Login subcommand parsing and dispatch for direct API-key entry
      contains: --api-key
    - path: code-rs/cli/src/login.rs
      provides: Provider-specific login helpers and stdin compatibility
      contains: run_login_with_api_key
    - path: code-rs/cli/tests/provider_api_key_entry.rs
      provides: CLI regression coverage for direct arg and stdin login
      contains: code login --api-key
    - path: docs/authentication.md
      provides: User-facing auth usage docs
      contains: --api-key
  key_links:
    - from: `Subcommand::Login`
      to: `run_login_with_api_key(...)`
      via: direct `--api-key` dispatch
      pattern: `api_key.is_some()`
    - from: `--with-api-key`
      to: `read_api_key_from_stdin()`
      via: piped stdin compatibility
      pattern: `with_api_key`
    - from: `run_login_with_api_key(...)`
      to: `login_with_api_key(...)` / `save_provider_api_key(...)`
      via: provider-specific storage dispatch
      pattern: `provider == "openai"`
---

<objective>
Restore direct CLI API-key entry without breaking the existing stdin flow.

Purpose: let scripted onboarding pass the key directly when needed while preserving the shell-safe stdin path that already exists.
Output: a working `code login --api-key <KEY>` path, unchanged stdin compatibility, focused CLI regression tests, and updated auth docs.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-credential-crud/04-CONTEXT.md
@.planning/phases/04-provider-credential-crud/04-DISCUSSION-LOG.md
@code-rs/cli/src/main.rs
@code-rs/cli/src/login.rs
@code-rs/core/src/auth.rs
@docs/authentication.md

<interfaces>
From `code-rs/cli/src/main.rs`:

```rust
struct LoginCommand {
    with_api_key: bool,
    provider: String,
    api_key: Option<String>,
}
```

From `code-rs/cli/src/login.rs`:

```rust
pub async fn run_login_with_api_key(
    cli_config_overrides: CliConfigOverrides,
    provider: String,
    api_key: String,
) -> !;
pub fn read_api_key_from_stdin() -> String;
```

From `code-rs/core/src/auth.rs`:

```rust
pub fn login_with_api_key(code_home: &Path, api_key: &str) -> std::io::Result<()>;
pub fn save_provider_api_key(code_home: &Path, provider_ref: &str, api_key: &str) -> std::io::Result<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore direct `--api-key` dispatch</name>
  <files>code-rs/cli/src/main.rs, code-rs/cli/src/login.rs</files>
  <read_first>code-rs/cli/src/main.rs, code-rs/cli/src/login.rs</read_first>
  <action>Make `--api-key <KEY>` the real direct-argument path per D-04, while keeping `--with-api-key` as the stdin path per D-05. Route the direct-argument branch into `run_login_with_api_key(...)`, preserve provider selection for OpenAI versus non-OpenAI providers, and keep the current `save_provider_api_key(...)` / `login_with_api_key(...)` split intact. Update help/error text so the CLI makes the two credential-entry modes explicit and does not mention the old deprecation behavior.</action>
  <verify>cargo test -p code-cli provider_api_key_entry -- --nocapture</verify>
  <done>`code login --api-key` works directly and the stdin pipe path still behaves exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI credential-entry regressions and auth docs</name>
  <files>code-rs/cli/tests/provider_api_key_entry.rs, docs/authentication.md</files>
  <read_first>code-rs/cli/tests/provider_api_key_entry.rs, docs/authentication.md</read_first>
  <action>Add focused integration tests that exercise both credential-entry modes: direct `--api-key` input and stdin-based `--with-api-key` input. Cover at least one OpenAI case and one non-OpenAI provider case so the tests prove the provider dispatch stays correct. Refresh `docs/authentication.md` to describe both supported modes, keeping the stdin guidance for shell-safe use and removing the stale note that the direct flag is unsupported.</action>
  <verify>cargo test -p code-cli provider_api_key_entry -- --nocapture</verify>
  <done>CLI onboarding is documented and regression-tested for both argument styles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- CLI arguments are visible in shell history and process listings.
- Stdin input is untrusted until it is trimmed and validated.
- Login help text and docs must not encourage accidental secret leakage.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-04-02-01 | information_disclosure | `code-rs/cli/src/main.rs`, `code-rs/cli/src/login.rs` | mitigate | Accept `--api-key` directly for scripted use, keep `--with-api-key` for stdin, and never echo the raw key in help text, logs, or errors. |
| T-04-02-02 | tampering | `code-rs/cli/src/login.rs` | mitigate | Normalize the provider id once and dispatch through the existing provider-specific auth helpers so the CLI cannot route a key to the wrong storage path. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p code-cli provider_api_key_entry -- --nocapture`
</verification>

<success_criteria>

- `code login --api-key <KEY>` works without piping stdin
- `code login --with-api-key` still reads from stdin
- OpenAI continues to use the existing OpenAI login path
- Non-OpenAI providers continue to use provider-key storage
- `docs/authentication.md` matches the implemented CLI behavior

</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-credential-crud/04-02-SUMMARY.md`
</output>
