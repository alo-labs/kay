---
phase: 10-boundary-audit-and-validation
plan: 1
type: execute
wave: 1
depends_on:
  - 09-runtime-paths-scripts-and-binary-renames
files_modified:
  - docs/kay-brand-renaming-inventory.md
  - docs/config.md
  - docs/agents.md
  - docs/advanced.md
  - docs/authentication.md
  - docs/prompts.md
  - docs/settings.md
  - docs/tui-stream-chunking-validation.md
  - docs/index.md
  - code-rs/cli/src/main.rs
autonomous: true
requirements:
  - COMP-02
  - SYNC-02
  - SYNC-03
must_haves:
  truths:
    - "Legacy names remain only where an external contract, generated artifact, or upstream comparison requires them."
    - "The rename inventory captures each retained boundary category and explains why it stays."
    - "The phase passes `./build-fast.sh` and the rename-sensitive regression checks relevant to the touched surfaces."
  artifacts:
    - path: "docs/kay-brand-renaming-inventory.md"
      provides: "Boundary inventory updated with retained-name categories"
    - path: "docs/config.md"
      provides: "Kay-first config guidance with compatibility aliases explained"
    - path: "docs/agents.md"
      provides: "Kay-first agent guidance with legacy env gating called out"
    - path: "docs/advanced.md"
      provides: "Kay-first debug/log guidance with compatibility notes"
    - path: "docs/authentication.md"
      provides: "Kay-home auth path guidance"
    - path: "docs/prompts.md"
      provides: "Kay-home prompt location guidance"
    - path: "docs/settings.md"
      provides: "Kay-home settings persistence guidance"
    - path: "docs/tui-stream-chunking-validation.md"
      provides: "Kay-first log path guidance for stream validation"
    - path: "docs/index.md"
      provides: "Governance links to the rename policy and inventory"
    - path: "code-rs/cli/src/main.rs"
      provides: "Kay-tui log replay help text"
---

<objective>
Audit the remaining brand-boundary surfaces, confirm that legacy names are retained only where they are still required, and validate the migration with the repo build gate plus rename-sensitive regression checks.

Purpose: This is the final boundary sweep for the Kay brand migration milestone. It records the explicit compatibility categories left in the tree and proves the renamed surfaces still build and run cleanly.
Output: Updated boundary inventory, verified docs/runtime naming boundaries, and phase validation evidence ready for milestone closure.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/kay-brand-renaming-policy.md
@docs/kay-brand-renaming-inventory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and document retained boundary names</name>
  <files>docs/kay-brand-renaming-inventory.md, docs/config.md, docs/agents.md, docs/advanced.md, docs/authentication.md, docs/prompts.md, docs/settings.md, docs/tui-stream-chunking-validation.md, docs/index.md, code-rs/cli/src/main.rs</files>
  <action>Review the remaining Kay migration boundary surfaces and ensure the inventory explains every retained legacy token category. Keep the user-facing docs Kay-first while documenting compatibility aliases, legacy env names, generated identifiers, and retained log prefixes where they are still required.</action>
  <verify>Compare the edited docs against the inventory and confirm each retained legacy token has a documented reason to exist.</verify>
  <done>The boundary inventory and docs clearly separate Kay-first surfaces from compatibility-bound legacy names.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the rename-sensitive surfaces</name>
  <files>build-fast.sh, code-rs/cli/tests/test_notes_app_live_e2e.rs, code-rs/tui/tests/vt100_chatwidget_snapshot.rs, docs/task-doc-checklist.json</files>
  <action>Run the repo build gate and the targeted regression checks that exercise the renamed runtime/documentation surfaces. Treat any failure or unexpected legacy-name regression as a gap to diagnose before milestone closure.</action>
  <verify>Confirm the build gate and targeted regression checks pass cleanly, then record the outcome in the phase verification and summary artifacts.</verify>
  <done>The rename batch is validated with the required build and regression checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build-fast.sh`
- [ ] `cargo test -p code-cli --test test_notes_app_live_e2e opencode_go_notes_app_live_feature_workflow -- --nocapture`
- [ ] `cargo test -p code-tui --test vt100_chatwidget_snapshot --features test-helpers -- --nocapture`
</verification>

<success_criteria>

- The remaining brand-boundary categories are documented in the inventory.
- Kay-first docs and CLI help text retain legacy names only where they are required for compatibility.
- The repo build gate and targeted regression checks pass cleanly.

</success_criteria>

