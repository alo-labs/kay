---
phase: 09-runtime-paths-scripts-and-binary-renames
plan: 1
type: execute
wave: 1
depends_on:
  - 08-kay-first-docs-and-copy-sweep
files_modified:
  - package.json
  - build-fast.sh
  - pre-release.sh
  - scripts/ci-tests.sh
  - scripts/generate-homebrew-formula.sh
  - scripts/post-release-cleanup.sh
  - scripts/test-post-release-cleanup.sh
  - code-rs/justfile
  - docs/config.md
  - docs/agents.md
  - docs/advanced.md
  - docs/slash-commands.md
  - docs/install.md
  - docs/homebrew.md
  - docs/integration-zed.md
  - docs/task-doc-checklist.json
  - docs/kay-brand-renaming-inventory.md
autonomous: true
requirements:
  - COMP-01
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "Runtime scripts and developer entrypoints use Kay-first command names where the public interface allows it."
    - "Compatibility aliases remain available where the binary or package identity is still shared with older workflows."
    - "Remaining config and agent docs explain Kay-first runtime paths while explicitly calling out compatibility-bound names."
    - "The rename inventory continues to track every retained compatibility token discovered in the runtime sweep."
  artifacts:
    - path: "package.json"
      provides: "Kay-first repo scripts and local build entrypoints"
    - path: "build-fast.sh"
      provides: "Kay-first build gate with compatibility aliases preserved"
    - path: "pre-release.sh"
      provides: "Kay-first pre-release build and smoke entrypoint"
    - path: "scripts/ci-tests.sh"
      provides: "Kay-first CI smoke wrapper"
    - path: "scripts/generate-homebrew-formula.sh"
      provides: "Kay-first release formula generation with compatibility shims"
    - path: "scripts/post-release-cleanup.sh"
      provides: "Kay-first cleanup path list"
    - path: "scripts/test-post-release-cleanup.sh"
      provides: "Cleanup regression coverage"
    - path: "code-rs/justfile"
      provides: "Kay-first just recipes for local developer flows"
    - path: "docs/config.md"
      provides: "Manual compatibility review for runtime config paths"
    - path: "docs/agents.md"
      provides: "Manual compatibility review for agent naming and built-ins"
    - path: "docs/advanced.md"
      provides: "Kay-first advanced/runtime guidance"
    - path: "docs/slash-commands.md"
      provides: "Kay-first command reference with legacy alias notes"
    - path: "docs/install.md"
      provides: "Kay-first install docs aligned with shipped binaries"
    - path: "docs/homebrew.md"
      provides: "Kay-first packaging docs aligned with shipped binaries"
    - path: "docs/integration-zed.md"
      provides: "Kay-first integration docs aligned with runtime paths"
    - path: "docs/task-doc-checklist.json"
      provides: "Docs freshness ledger synchronized with this sweep"
    - path: "docs/kay-brand-renaming-inventory.md"
      provides: "Updated inventory of retained compatibility names"
  key_links:
    - from: "build-fast.sh"
      to: "scripts/check-codex-path-deps.sh"
      via: "path dependency guard"
      pattern: "check-codex-path-deps"
    - from: "scripts/generate-homebrew-formula.sh"
      to: "scripts/install/install.sh"
      via: "Kay release/install naming"
      pattern: "Kay"
    - from: "docs/slash-commands.md"
      to: "code-rs/core/src/slash_commands.rs"
      via: "command reference"
      pattern: "/kay"
---

<objective>
Rename the runtime-facing command and script surfaces to Kay-first names where the public interface permits it, while preserving compatibility aliases for older workflows and manually reviewing the config/agent docs that still need boundary-aware wording.

Purpose: This is the runtime-path sweep in the Kay brand migration. It moves developer entrypoints, release helpers, and the command reference toward Kay naming without breaking current automation.
Output: Updated scripts, justfile recipes, and compatibility-aware docs, plus a refreshed inventory entry for anything that intentionally remains legacy.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/kay-brand-renaming-policy.md
@docs/kay-brand-renaming-inventory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename runtime scripts and developer entrypoints to Kay-first names</name>
  <files>package.json, build-fast.sh, pre-release.sh, scripts/ci-tests.sh, scripts/generate-homebrew-formula.sh, scripts/post-release-cleanup.sh, scripts/test-post-release-cleanup.sh, code-rs/justfile</files>
  <action>Update the user-visible command names, help text, and shell entrypoints to prefer Kay terminology while keeping compatibility aliases and fallback paths where older automation still depends on them. Keep the behavior of the build and smoke gates intact, but make the primary wording and invocation paths Kay-first.</action>
  <verify>Review the edited scripts for any accidental loss of fallback behavior, then run the required repo gate.</verify>
  <done>The runtime and developer entrypoints read as Kay-first without breaking legacy aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Manually review the remaining config and agent docs</name>
  <files>docs/config.md, docs/agents.md, docs/advanced.md, docs/slash-commands.md, docs/install.md, docs/homebrew.md, docs/integration-zed.md, docs/task-doc-checklist.json, docs/kay-brand-renaming-inventory.md</files>
  <action>Rewrite the remaining docs so they describe Kay runtime paths, command names, and packaging in Kay-first language while keeping compatibility-bound environment variables, model slugs, and upstream names only where they are still required. Refresh the inventory when a retained legacy token is intentional.</action>
  <verify>Compare the edited docs against the inventory and confirm every retained legacy token has a documented reason to exist.</verify>
  <done>The docs reflect the Kay runtime naming direction and the inventory records any deliberate exceptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build-fast.sh`
</verification>

<success_criteria>

- Runtime scripts and the justfile prefer Kay names where the interface allows it.
- Legacy aliases remain functional for older workflows.
- Remaining config and agent docs are Kay-first and explain compatibility boundaries explicitly.
- The rename inventory matches the actual retained legacy names.
- `./build-fast.sh` passes cleanly.

</success_criteria>

<output>
After completion, create `.planning/phases/09-runtime-paths-scripts-and-binary-renames/09-01-SUMMARY.md`
</output>
