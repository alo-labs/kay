---
phase: 05-dynamic-model-selection
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - code-rs/tui/src/chatwidget.rs
  - code-rs/tui/src/bottom_pane/model_selection_view.rs
  - code-rs/tui/src/chatwidget/smoke_helpers.rs
  - code-rs/tui/tests/vt100_chatwidget_snapshot.rs
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
must_haves:
  truths:
    - "`/model` only receives model presets for providers whose credentials are actually available."
    - "OpenCode Go, MiniMax, and OpenAI appear in the locked provider order, with the OpenAI shortlist staying inside its own bucket."
    - "The picker still preserves reasoning-effort choices and model detail rendering."
    - "When nothing is visible, the picker shows a clear onboarding or empty-state hint rather than a blank list."
  artifacts:
    - path: "code-rs/tui/src/chatwidget.rs"
      provides: "Shared picker wiring that consumes the reusable visibility helper"
      contains: "code_core::model_visibility"
    - path: "code-rs/tui/src/bottom_pane/model_selection_view.rs"
      provides: "Provider-aware picker rendering and empty state"
      min_lines: 150
    - path: "code-rs/tui/src/chatwidget/smoke_helpers.rs"
      provides: "Test-only helper that opens the real model picker"
      contains: "open_model_selection_overlay_with_presets"
    - path: "code-rs/tui/tests/vt100_chatwidget_snapshot.rs"
      provides: "Live VT100 coverage for credentialed and empty-state picker states"
      contains: "model_selection_visibility"
  key_links:
    - from: "code-rs/tui/src/chatwidget.rs"
      to: "code-rs/core/src/model_visibility.rs"
      via: "shared visibility helper"
      pattern: "code_core::model_visibility"
    - from: "code-rs/tui/src/bottom_pane/model_selection_view.rs"
      to: "code-rs/core/src/model_family.rs"
      via: "provider_model_slug"
      pattern: "provider_model_slug\\("
    - from: "code-rs/tui/src/chatwidget/smoke_helpers.rs"
      to: "code-rs/tui/tests/vt100_chatwidget_snapshot.rs"
      via: "test-only picker setup"
      pattern: "open_model_selection_overlay_with_presets"
---

<objective>
Wire the reusable provider visibility helper into the TUI model picker and prove the real `/model` experience with unit and VT100 coverage.

Purpose: This is the user-facing Phase 5 slice that makes `/model` hide locked providers while keeping the picker readable and reusable.
Output: Picker wiring, provider-aware rendering, a real smoke helper, and snapshots for the credentialed and empty-provider states.
</objective>

<execution_context>
@/Users/shafqat/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dynamic-model-selection/05-CONTEXT.md
@code-rs/tui/src/chatwidget.rs
@code-rs/tui/src/bottom_pane/model_selection_view.rs
@code-rs/tui/src/chatwidget/smoke_helpers.rs
@code-rs/tui/tests/vt100_chatwidget_snapshot.rs
@code-rs/core/src/model_family.rs
@code-rs/core/src/model_visibility.rs

<interfaces>
From `code-rs/core/src/model_visibility.rs` as created in wave 1:
```rust
pub enum VisibleProvider {
    OpenCodeGo,
    MiniMax,
    OpenAI,
}

pub struct VisibleProviderModels {
    pub provider: VisibleProvider,
    pub label: &'static str,
    pub presets: Vec<ModelPreset>,
}

pub fn visible_model_groups(
    auth: &AuthManager,
    presets: &[ModelPreset],
) -> Vec<VisibleProviderModels>;

pub fn visible_model_presets(
    auth: &AuthManager,
    presets: &[ModelPreset],
) -> Vec<ModelPreset>;
```

From `code-rs/core/src/model_family.rs`:
```rust
pub fn provider_model_slug<'a>(provider_id: &str, model_slug: &'a str) -> Cow<'a, str>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route ChatWidget preset loading through the shared visibility helper</name>
  <files>code-rs/tui/src/chatwidget.rs</files>
  <behavior>
    - The local picker path and remote preset refresh path both use the same provider-aware helper, so `/model` and the other picker entry points see identical visibility rules.
    - Existing curated ordering, remote-default selection, and reasoning-effort handling stay intact within the visible provider buckets.
    - When the visible preset list is empty, the UI can fall through to the picker's empty state instead of exposing a broken, unfiltered list.
  </behavior>
  <action>Replace the current preset selection flow so `available_model_presets()` and `update_model_presets()` consume the shared `code_core::model_visibility` helper. Keep the OpenAI shortlist behavior only inside the OpenAI bucket, preserve the remote-default application path, and avoid reintroducing provider CRUD or per-SKU filtering in the picker code. `/model` should be the primary consumer of the shared visible list, and every picker entry point should receive the same auth-driven provider visibility rules.</action>
  <verify>
    <automated>cargo test -p code-tui available_model_presets_filters_provider_visibility -- --nocapture</automated>
  </verify>
  <done>`/model` and the shared picker entry points only operate on provider-visible presets returned by the reusable core helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render provider sections and empty state in the model picker</name>
  <files>code-rs/tui/src/bottom_pane/model_selection_view.rs</files>
  <behavior>
    - Provider headers or labels appear in the locked order OpenCode Go, MiniMax, OpenAI.
    - Namespaced provider-local slugs are normalized with `provider_model_slug(...)` for display, but selection still resolves the full preset.
    - Reasoning-effort rows, model detail text, and current-selection markers continue to render exactly as before inside each provider bucket.
    - The empty-state/onboarding hint appears when the filtered preset set is empty, instead of rendering an apparently broken list.
  </behavior>
  <action>Refactor the picker to carry provider metadata alongside each preset, group rows by provider bucket, and update `rendered_rows`, `content_line_count`, sorting, and selection movement so the new headers are non-selectable. Reuse `provider_model_slug(...)` from `code_core::model_family` for header normalization, but keep the actual model identifiers untouched when a preset is chosen. Use the wave 1 helper as the source of truth for visibility so the picker does not need provider-specific per-SKU exceptions.</action>
  <verify>
    <automated>cargo test -p code-tui model_selection_view -- --nocapture</automated>
  </verify>
  <done>The picker visibly distinguishes provider buckets, keeps its existing reasoning-effort UX, and shows a clear empty state when nothing is unlocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add smoke-helper and VT100 coverage for the live `/model` picker</name>
  <files>code-rs/tui/src/chatwidget/smoke_helpers.rs, code-rs/tui/tests/vt100_chatwidget_snapshot.rs</files>
  <behavior>
    - The smoke helper opens the real `/model` picker with seeded presets, not a fake rendering path.
    - VT100 snapshots cover both a credentialed-provider list and the no-credential onboarding hint.
    - Snapshot output proves the grouped provider order and the preserved reasoning-effort rows from the live picker.
  </behavior>
  <action>Add a test-only helper that seeds the picker with model presets and opens the actual model-selection overlay, then capture VT100 snapshots for a representative unlocked provider set and an empty credentials state. Keep the helper behind the existing test-helpers gate and use it to prove the end-to-end `/model` flow matches the core helper and picker rendering logic from the earlier tasks.</action>
  <verify>
    <automated>cargo test -p code-tui --test vt100_chatwidget_snapshot --features test-helpers model_selection_visibility -- --nocapture</automated>
  </verify>
  <done>End-to-end snapshots lock in the credential-aware `/model` experience, the empty-state hint, and the provider ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core helper -> TUI render path | Auth-derived visibility data is turned into visible picker rows. |
| user command input -> model selection | Free-form `/model <name>` input still lands on the model picker and selection logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S | model headers / provider labels | mitigate | Render provider labels from the helper enum and normalize slugs with `provider_model_slug(...)`, not from user-provided strings. |
| T-05-05 | I | empty-state handling | mitigate | When no provider is unlocked, show an explicit onboarding hint and avoid rendering a misleading blank list. |
| T-05-06 | T | snapshot harness | accept | The smoke helper is test-only and feature-gated, so it cannot alter runtime behavior outside VT100 regression coverage. |
</threat_model>

<verification>
The TUI slice is complete when the targeted `code-tui` unit tests and VT100 snapshots pass, and the live picker renders the same provider-aware list as the reusable core helper.
</verification>

<success_criteria>
`/model` shows only credential-enabled providers, provider order is stable, reasoning-effort rows remain intact, and the empty-state hint is covered by a real smoke path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dynamic-model-selection/05-02-SUMMARY.md` describing the TUI wiring, provider grouping, and VT100 snapshot coverage.
</output>
