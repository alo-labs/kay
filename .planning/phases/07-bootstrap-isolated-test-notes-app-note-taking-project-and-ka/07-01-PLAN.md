---
phase: 07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code-rs/arg0/src/lib.rs
  - code-rs/core/src/config/sources.rs
  - code-rs/core/src/message_history.rs
  - code-rs/tui/src/session_log.rs
  - code-rs/core/tests/custom_prompts_discovery.rs
  - code-rs/cli/tests/provider_api_key_entry.rs
  - code-rs/tui/tests/vt100_chatwidget_snapshot.rs
  - scripts/install/install.sh
  - scripts/install/install.ps1
  - docs/config.md
  - docs/authentication.md
  - docs/auto-drive.md
  - docs/getting-started.md
  - docs/advanced.md
  - docs/execpolicy.md
  - docs/prompts.md
  - docs/settings.md
autonomous: true
requirements:
  - KAY-01
  - KAY-02
  - DOCS-01
user_setup: []
must_haves:
  truths:
    - "Kay defaults to ~/.kay when no explicit home override is supplied."
    - "Kay no longer auto-inherits local ~/.codex state during startup."
    - "Transcript and history persistence continue to work under the isolated Kay home."
  artifacts:
    - path: "code-rs/core/src/config/sources.rs"
      provides: "Kay home resolution and path read defaults"
    - path: "code-rs/arg0/src/lib.rs"
      provides: "Startup dotenv loading and env bridging"
    - path: "code-rs/core/src/message_history.rs"
      provides: "History JSONL persistence under the resolved Kay home"
    - path: "code-rs/tui/src/session_log.rs"
      provides: "Per-session transcript JSONL logging"
  key_links:
    - from: "code-rs/arg0/src/lib.rs"
      to: "code-rs/core/src/config/sources.rs"
      via: "startup resolves the active home before loading dotenv"
      pattern: "find_code_home\\(\\)"
    - from: "code-rs/core/src/message_history.rs"
      to: "code-rs/core/src/config/sources.rs"
      via: "history path follows the resolved home"
      pattern: "history_filepath"
---

<objective>
Make Kay's end-user runtime self-contained under `~/.kay` and remove the automatic local Codex/Every Code inheritance path.

Purpose: The Kay install must no longer silently drift into an existing Codex home. This is the isolation foundation for the rest of the milestone.
Output: Updated home-resolution code, startup env loading, persistence paths, install defaults, docs, and regression coverage.
</objective>

<threat_model>
Potential threats:
- Accidental reads/writes to `~/.code` or `~/.codex` could leak user state across products.
- Startup dotenv inheritance could silently import the wrong credentials or environment settings.
- Doc drift could mislead users into installing Kay into the wrong home directory.
</threat_model>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka/07-CONTEXT.md

@code-rs/core/src/config/sources.rs
@code-rs/arg0/src/lib.rs
@code-rs/core/src/message_history.rs
@code-rs/tui/src/session_log.rs
@code-rs/cli/tests/provider_api_key_entry.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget Kay home resolution to ~/.kay</name>
  <files>code-rs/core/src/config/sources.rs, code-rs/arg0/src/lib.rs, code-rs/core/src/config.rs</files>
  <read_first>code-rs/core/src/config/sources.rs, code-rs/arg0/src/lib.rs</read_first>
  <action>Change the default writable home to `~/.kay`, remove the automatic `~/.codex` fallback from startup path resolution, and stop backfilling `CODEX_HOME` from `CODE_HOME`. Keep explicit user-supplied overrides working, but make the default Kay install isolated.</action>
  <verify>rg -n \"legacy_code_home_dir|~/.codex|CODEX_HOME.*CODE_HOME\" code-rs/core/src/config/sources.rs code-rs/arg0/src/lib.rs</verify>
  <acceptance_criteria>
    - `find_code_home()` defaults to `~/.kay` when no override is present.
    - Kay startup no longer auto-loads legacy local Codex env/state.
    - The `CODE_HOME` to `CODEX_HOME` inheritance bridge is gone.
  </acceptance_criteria>
  <done>Kay resolves an isolated home without silently inheriting local Codex state.</done>
</task>

<task type="auto">
  <name>Task 2: Update isolated persistence paths and bootstrap docs</name>
  <files>code-rs/core/src/message_history.rs, code-rs/tui/src/session_log.rs, code-rs/core/tests/custom_prompts_discovery.rs, code-rs/cli/tests/provider_api_key_entry.rs, code-rs/tui/tests/vt100_chatwidget_snapshot.rs, scripts/install/install.sh, scripts/install/install.ps1, docs/config.md, docs/authentication.md, docs/auto-drive.md, docs/getting-started.md, docs/advanced.md, docs/execpolicy.md, docs/prompts.md, docs/settings.md</files>
  <read_first>code-rs/core/src/message_history.rs, code-rs/tui/src/session_log.rs, docs/config.md, docs/authentication.md</read_first>
  <action>Update every user-facing path and install-time default that still points at `~/.code`/`~/.codex` so the Kay install story consistently uses the isolated home, transcript/history persistence stays in the Kay home, and the docs stop implying that local Codex state is inherited by default.</action>
  <verify>rg -n \"~/.code|~/.codex\" docs scripts/install code-rs/core/src/message_history.rs code-rs/tui/src/session_log.rs | head -n 80</verify>
  <acceptance_criteria>
    - Documentation consistently describes Kay's isolated home and transcript/history paths.
    - Install scripts default new Kay state into `~/.kay`.
    - Regression tests still pass for key entry and transcript persistence paths.
  </acceptance_criteria>
  <done>Kay's persistence surfaces and docs tell the same isolated-home story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build-fast.sh`
- [ ] Targeted Kay home / key-entry / transcript tests pass
- [ ] Docs grep no longer shows legacy `~/.code` / `~/.codex` defaults where Kay's end-user install should be isolated
</verification>

<success_criteria>

- Kay end-user state defaults to `~/.kay` and no longer silently inherits local Codex/Every Code state.
- The isolated Kay install story is documented and verifiable.
- No errors or warnings introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka/07-01-SUMMARY.md`
</output>
