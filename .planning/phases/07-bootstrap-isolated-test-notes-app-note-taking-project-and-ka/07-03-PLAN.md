---
phase: 07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - scripts/bootstrap-test-notes-app.sh
  - code-rs/cli/tests/test_notes_app_live_e2e.rs
  - docs/test-notes-app.md
  - docs/TESTING.md
autonomous: true
requirements:
  - NOTE-01
  - TEST-01
  - DOCS-01
  - REL-01
user_setup: []
must_haves:
  truths:
    - "A real alo-exp/test-notes-app repo exists and can be checked out locally under projects/test-notes-app."
    - "Kay can run meaningful OpenCode Go model work against the note app, not just trivial smokes."
    - "The bootstrap and validation workflow is repeatable for future releases."
  artifacts:
    - path: "scripts/bootstrap-test-notes-app.sh"
      provides: "Repeatable repo/bootstrap automation for the note app target"
    - path: "code-rs/cli/tests/test_notes_app_live_e2e.rs"
      provides: "Live validation harness for the note app"
    - path: "docs/test-notes-app.md"
      provides: "Workflow instructions and provenance notes"
  key_links:
    - from: "scripts/bootstrap-test-notes-app.sh"
      to: "code-rs/cli/tests/test_notes_app_live_e2e.rs"
      via: "bootstrap script creates the repo that the live test targets"
      pattern: "test-notes-app"
    - from: "code-rs/cli/tests/test_notes_app_live_e2e.rs"
      to: "docs/TESTING.md"
      via: "release validation references the live test harness"
      pattern: "provider_model_acceptance|test_notes_app"
---

<objective>
Bootstrap the real `alo-exp/test-notes-app` repository and add the live OpenCode Go validation harness that will be exercised on substantial note-app work before each release.

Purpose: Kay needs a stable real-world target project so model quality is proven by useful work instead of only smoke prompts.
Output: A repo bootstrap script, a live note-app validation harness, and the docs needed to run it repeatably before release.
</objective>

<threat_model>
Potential threats:
- The bootstrap flow could leak API keys, tokens, or session data into the new repo or its logs.
- The live harness could accidentally run against the wrong checkout or the wrong provider key.
- A poorly scoped scaffold could create a toy demo instead of a real project target.
</threat_model>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka/07-CONTEXT.md

@code-rs/cli/tests/provider_model_acceptance.rs
@code-rs/cli/tests/opencode_go_live_e2e.rs
@code-rs/cli/tests/minimax_live_e2e.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeatable note-app bootstrap automation</name>
  <files>scripts/bootstrap-test-notes-app.sh, docs/test-notes-app.md</files>
  <read_first>code-rs/cli/tests/provider_model_acceptance.rs, docs/TESTING.md</read_first>
  <action>Create a script and companion docs that can provision the real `alo-exp/test-notes-app` repo and local checkout under `projects/test-notes-app`, with Kay-local environment setup and no dependence on the existing Codex local environment.</action>
  <verify>test -x scripts/bootstrap-test-notes-app.sh && scripts/bootstrap-test-notes-app.sh --help</verify>
  <acceptance_criteria>
    - The bootstrap script exists and is actionable.
    - The docs explain the repo target, local path, and Kay-isolated setup.
    - The script does not write secrets to the repo or docs.
  </acceptance_criteria>
  <done>The note-app bootstrap can be repeated without hand-crafted setup steps.</done>
</task>

<task type="auto">
  <name>Task 2: Add live OCG note-app validation harness</name>
  <files>code-rs/cli/tests/test_notes_app_live_e2e.rs, docs/TESTING.md</files>
  <read_first>code-rs/cli/tests/provider_model_acceptance.rs, code-rs/cli/tests/opencode_go_live_e2e.rs</read_first>
  <action>Add a live end-to-end test harness that uses the note app as the real target project and exercises the supported OpenCode Go models on meaningful work, with transcript capture for later provenance review.</action>
  <verify>cargo test -p code-cli --test test_notes_app_live_e2e -- --nocapture</verify>
  <acceptance_criteria>
    - The live harness targets the note app, not a throwaway smoke prompt.
    - Each supported OpenCode Go model can be driven through substantial work.
    - The harness preserves transcript/provenance output for later analysis.
  </acceptance_criteria>
  <done>The note-app validation harness proves Kay on real work across the OCG set.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build-fast.sh`
- [ ] The note-app bootstrap and live validation harness both work
- [ ] The supported OCG matrix covers meaningful note-app work, not only smoke prompts
</verification>

<success_criteria>

- A real `alo-exp/test-notes-app` repo exists and is wired into repeatable Kay validation.
- The harness exercises the supported OpenCode Go models on useful note-app work before releases.
- No errors or warnings introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bootstrap-isolated-test-notes-app-note-taking-project-and-ka/07-03-SUMMARY.md`
</output>
