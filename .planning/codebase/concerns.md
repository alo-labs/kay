# Concerns

## Summary

These are the current brownfield risks worth keeping visible while the project is being oriented.

## Current Concerns

- The repo has both product code and a large docs surface, so initialization must stay non-destructive
- Silver Bullet / GSD compatibility depends on local cache paths and user-home settings
- The repo already has existing instructions and workflows, so new guidance must layer cleanly
- Build-time warnings should be treated as failures during completion

## Follow-Up

- Revisit the brownfield codebase map after the first real development phase
- Add more specific concerns once the feature roadmap is known
