# Knowledge - 2026-05

## Architecture Patterns

- Silver Bullet init for a brownfield repo should preserve existing instructions and docs, then layer new enforcement artifacts alongside them.
- GSD on this machine needs a shared runtime catalog in both `~/.claude/sdk/shared/` and `~/.kay/sdk/shared/` to satisfy the local compatibility shim.
- Phase 1 stays cleaner when it is interface-first and leaves docs, live validation, and release work for later phases.
- Built-in provider registration and wire-slug normalization should move together so the active provider id and outbound request shape stay aligned.
- Kay slash-command renames should update the TUI enum, the formatter, and the user-facing docs together; otherwise the UI can show Kay while the prompt-expanding path still remains inconsistent underneath.
- Live acceptance repos used for Kay should be cloned from a trusted git checkout with a clean HEAD baseline; otherwise a local working tree can leak into the model's test context.
- Model-family prompt adaptations are the right seam for model-specific agent behavior fixes when the provider wire format is already correct; MiMo now adds a synthesis checkpoint without changing OpenCode Go transport behavior.
- OpenCode Go live streams need a shorter idle timeout than the generic provider default so a silent provider-side stall retries within Kay's live validation window.
- MiMo prompts should avoid the full skills catalog unless a task explicitly needs skills; large discoverability blocks can crowd out simple file-inspection behavior.
- Direct Xiaomi MiMo support can reuse the OpenAI-compatible chat transport and MiMo model-family profile, but it still needs separate provider credentials, picker visibility, and live acceptance coverage because the provider id is different from OpenCode Go.
- MiMo tool-use compatibility belongs in the shared `mimo` family profile: the same apply-patch grammar, single-tool-call JSON discipline, and final-output contract rules should apply to both OpenCode Go and direct Xiaomi namespaces.
- Structured-output schema handling has to account for both model family and tool availability on Chat Completions providers: MiMo should use shared bounded final-output schema guidance instead of native `response_format: json_schema`, while other no-tool families can still use native schema formatting.
- MiMo can still violate single-tool JSON guidance by concatenating multiple shell argument objects into one tool call, and may encode each `command` as a command string instead of an argv array; recover both in the shared shell-tool parser so all MiMo namespaces benefit without provider-specific branches.
- Direct Xiaomi MiMo uses the standard five-minute stream idle timeout because `mimo-v2.5` can stay silent longer than OpenCode Go's one-minute window before its first SSE token.
- MiMo may return explanatory prose before a final schema-valid JSON object even after a strict output contract; harnesses should extract only a trailing contract object and still enforce exact fields plus real file diffs.
- MiMo can stall by retrying the same failed `apply_patch` hunk after context mismatches; family guidance should tell it to reread once, then use a smaller exact patch or bounded rewrite.
- MiMo can emit `apply_patch` hunk headers like `@@ hint paragraph @@`; the trailing `@@` is interpreted as bad context, so normalize that recoverable shape in the shared shell-tool path and explicitly forbid it in MiMo instructions.
- MiMo final-output schema enforcement belongs at the model-family turn layer: if a MiMo tool workflow emits progress prose where final JSON is required, inject a developer repair message and continue the same turn instead of completing with an invalid last message.
- Pre-release workspace tests should run without live-provider opt-in environment variables; the dedicated live-provider gate owns real model traffic and can load credentials from env aliases or auth.json.
- Exact named skill/workflow requests belong in the skills-rendered instruction
  contract. If the user requests `silver:init`, the model should execute that
  named workflow and not substitute discovery or scan paths unless the named
  skill itself routes there.
- MiniMax-M3 can emit malformed shell probes like `ls && -la && <path>` during
  file-edit workflows. The shared command normalizer is the right seam for
  deterministic repairs of this shape because it benefits every provider path
  before execution.

## Known Gotchas

- `gsd-sdk` can fail if the higher-level wrapper package is missing a Node dependency, even when `gsd-tools` is still usable locally.
- `docs/` may already contain a mature documentation set; init should add canonical placeholders without deleting the existing docs tree.
- The docs-scheme gate can still block completion if the governed docs were not refreshed in the same session, even when the main task is otherwise done.
- Reserving a new built-in provider id can break custom config and rollout replay if the reserved-name guard and migration story are not updated together.
- The docs-scheme ledger has to list every governed doc key, not just the files touched by the main feature change, or the completion hook will keep blocking the task.
- The final Kay slash-command review needed a second docs-scheme refresh pass so the checklist timestamp and the governed docs landed in the same session.
- For live test fixtures, SB session markers under both `.claude` and `.Codex` need to agree with the current session or the docs gate keeps reporting stale coverage.
- OpenCode Go model families can need behavior guidance even when request routing and response-model metadata match; treat repeated file rereads and missing synthesis as an instruction-profile issue before changing provider plumbing.
- A clean single live pass is not enough for model-behavior fixes; the full release gate can still expose nondeterministic stalls that require transport-level retry tuning.
- If a model stalls before the first tool call, inspect the assembled prompt size and sections; behavior may improve by reducing optional instruction catalogs rather than adding more guidance.
- A provider can pass transport-level chat completions while still failing an agentic workflow through early assistant progress messages or incomplete file edits; live tests should assert tracked diffs and behavior markers, not just final JSON.
- MiMo may wrap final JSON in a fenced block even when the prompt asks for plain JSON; harnesses should parse the contracted object without falling back to inferred file edits.
- A parse-error tool output is not always enough for Xiaomi MiMo recovery; malformed tool arguments can be followed by idle SSE retries, so recoverable malformed arguments should be normalized before handing control back to the model.
- Repeated idle SSE retries before the first MiMo token can be a provider timeout mismatch, not model refusal; check provider idle windows before adding more prompt guidance.
- `node --check` is not enough for generated JavaScript workflow tests: nested function declarations can be syntactically valid while leaving event handlers unable to call the intended function.
- Repeated patch-context failures should be treated as a model-behavior loop, not a transport error; prompts need an explicit escape path that still keeps edits scoped.
- Live JS behavior assertions should recognize equivalent typing-guard names such as `typing` and `isTyping`; otherwise a semantically correct generated shortcut can fail the harness for naming style rather than behavior.
- Exporting live provider keys into a broad test process can silently opt expensive live suites into deterministic workspace test phases; sanitize the environment at the phase boundary instead of weakening the live tests.
- Direct Xiaomi MiMo can repeatedly disconnect on native Chat Completions `response_format: json_schema` even after plain and developer-message calls succeed; keep that compatibility decision in the shared MiMo family instead of branching on the Xiaomi provider id.
- `kay exec` must distinguish recovered stream-retry notifications from fatal errors; otherwise a turn can complete with the expected final assistant message while automation still receives exit code 1.
- OpenCode Go MiMo live-smoke turns can need more than one five-minute SSE retry window; the release gate should budget enough time for recovery instead of dropping curated model coverage.
- `kay exec` compatibility flags must be tested on the exec subcommand itself,
  not only on the interactive root CLI; clap flattening does not imply
  subcommands inherit all root flags.

## Key Decisions

- Keep the repo's existing Rust build gate as the completion check.
- Use the brownfield-safe, non-destructive path for all init scaffolding.
- Keep the OpenCode Go milestone's first phase foundation-only and modular.
- Reserve `opencode-go` as a built-in provider id and keep login guidance plus resume compatibility aligned with that choice.
- Treat `/kay` as the canonical prompt-expanding slash command for the UI and formatter path.
- Use trusted `git clone --no-local` for seeded live E2E repos so the model sees a reproducible baseline and not the source checkout's working tree.
- Keep MiMo anti-loop guidance scoped to the MiMo model family so stronger OpenCode Go families do not inherit unnecessary behavioral constraints.
- Keep OpenCode Go idle-retry tuning at the provider boundary, not per test, so real users benefit from the same recovery path as the live gate.
- Keep MiMo context dieting scoped to optional skills discovery so AGENTS.md and project-specific safety instructions still apply.
- Treat Xiaomi as its own provider namespace (`xiaomi/<model>`) even when the model names overlap with OpenCode Go's MiMo slugs.
- Keep Xiaomi MiMo response parsing common with other OpenAI-compatible MiMo providers, and put provider-specific coverage around credentials, model-slug stripping, and live notes-app acceptance.
- Forward `kay exec --output-schema` into core turn context with an 8 KiB schema-file cap, but gate Chat Completions `response_format` by model-family support; MiMo uses shared schema guidance and real edit workflows validate final JSON plus actual diffs.
- Normalize concatenated shell-tool argument objects at the shared tool-call parsing layer by accepting argv arrays or command strings, then quoting each normalized argv vector into one shell script, preserving the common MiMo family/provider layering.
- Keep stream idle timeout provider-specific: direct Xiaomi gets a longer idle window, while shared MiMo behavior and response normalization remain in the model-family/tool layers.
- Keep recoverable `apply_patch` compatibility in the shell-tool normalization layer when the malformed patch text is deterministic, and keep behavioral anti-loop guidance in the MiMo family profile.
- Keep final-output schema repair scoped to the shared MiMo family so direct Xiaomi and OpenCode Go MiMo benefit from the same recovery while other model families keep the standard assistant-message completion behavior.
- Keep live provider credentials isolated to `scripts/pre-release-live-provider-gate.sh` during release preflight so workspace nextest remains a deterministic suite and live checks remain explicit.
- Keep headless exec defaults conservative, but accept explicit approval-policy
  overrides on the exec subcommand so documented root CLI compatibility flags
  remain usable in automation.
- Keep exact-skill routing as a prompt contract rather than a slash-command
  enum change; external workflow names such as `silver:init` are skill/plugin
  requests, not Kay built-in slash commands.
